# Copy to .env and customize locally. Do not commit .env.

# Local-only Ollama endpoint (loopback)
OLLAMA_HOST=http://127.0.0.1:11434

# Optional overrides (defaults are repo-relative: ./logs and ./exports)
# LOG_DIR=/absolute/or/repo/relative/path
# EXPORT_DIR=/absolute/or/repo/relative/path
