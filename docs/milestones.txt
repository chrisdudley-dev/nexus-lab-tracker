Nexus Lab Tracker — Milestones
Updated: 2026-02-12

M0 — FastAPI parity shell (local) [DONE]
- lims/api_fastapi.py runs under uvicorn
- /health returns schema + git_rev + db_path
- /metrics returns Prometheus metrics (db up + counts)
- POST /auth/guest creates session
- /sample/list requires X-Nexus-Session when NEXUS_REQUIRE_AUTH_FOR_SAMPLES=1

M1 — Interactive-safe dev scripts [DONE]
- scripts/run_fastapi.sh (detached + pidfile + idempotent)
- scripts/stop_fastapi.sh (pidfile-based stop)

M2 — systemd service [DONE]
- scripts/run_fastapi_systemd.sh foreground runner
- /etc/systemd/system/nexus-fastapi.service Type=simple
- Drop-in override sets NEXUS_REQUIRE_AUTH_FOR_SAMPLES=1
- Env verified in running process (/proc/<pid>/environ shows REPO_ROOT/DB_PATH/EXPORT_DIR/LOG_DIR)

M3 — Reverse proxy + stable exposure [NEXT]
- Add nginx/caddy in front of 127.0.0.1:8789
- Keep /metrics local-only or allowlist

M4 — Web UI wiring [NEXT]
- Decide UI serve strategy (static build vs separate dev server)
- Add basic page calling /health + /sample/list

M5 — Expand API beyond read/list [NEXT]
- Sample create/update/status transitions
- Container CRUD + linking
- Event append/query

M6 — Auth hardening [NEXT]
- Session persistence policy + expiry/rotation
- Rate limiting basics

M7 — Observability + ops [NEXT]
- Prometheus scrape + alert hooks (optional)
- Runbook entries for journalctl/status patterns

